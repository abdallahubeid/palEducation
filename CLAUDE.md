# CLAUDE.md — palEducation

منصة تعليمية إلكترونية لطلبة الثانوية العامة (التوجيهي) في فلسطين.
المطوّر يعمل **منفرداً**؛ Claude هو المساعد الوحيد. لغة التواصل: **العربية**.

---

## 0. اقرأ أولاً

| الملف | متى تقرأه |
|---|---|
| `DESIGN.md` | **قبل كتابة أي HTML/CSS/Blade.** نظام التصميم الكامل والـ tokens |
| `docs/01-project-understanding.md` | الأدوار، الصلاحيات، الشاشات، الرحلات، القرارات المعلّقة |
| `.claude/rules/*.md` | قواعد تفصيلية لكل مجال (RTL، Livewire، Laravel) |
| `.claude/design-references/` | 5 أنظمة تصميم مرجعية — **للإلهام البنيوي لا للنسخ** |

---

## 1. الستاك

```
Laravel 13.17 · PHP 8.3 · Livewire 3 · Tailwind CSS 4 · Vite 8 · Pest 5 · MySQL (XAMPP)
```

- Tailwind 4 يُهيّأ عبر `@theme` في `resources/css/app.css` — **لا `tailwind.config.js`**
- Blade + Livewire. **لا React، لا Vue، لا Inertia.**
- Alpine.js يأتي مع Livewire 3 — استخدمه للتفاعلات الخفيفة فقط

---

## 2. المبدآن الحاكمان

كل قرار في هذا المشروع يُقاس عليهما:

1. **الإقصاء المخصص** — الطالب لا يرى محتوى خارج فرعه. الفرع فلتر جذري على كل استعلام وكل واجهة.
2. **الحلقة المغلقة** — انتهاء الفيديو **حدث** يفتح الكويز تلقائياً. لا يضغط الطالب "ابدأ الاختبار".

---

## 3. غير قابل للتفاوض

### 🔴 الاتجاه (RTL/LTR)
**لا يوجد `left` أو `right` في هذا المشروع.** المنصة ثنائية اللغة (عربي RTL أساسي / إنجليزي LTR).

```
❌ ml-4  mr-4  pl-6  pr-6  text-left  text-right  border-l  left-0  float-left
✅ ms-4  me-4  ps-6  pe-6  text-start text-end    border-s  start-0 float-start
```

**استثناءات تبقى LTR دائماً:** مشغّل الفيديو (حاويته `dir="ltr"`) · الأرقام والتواريخ · المعادلات الرياضية والكود.

التفاصيل الكاملة: `.claude/rules/rtl-bilingual.md`

### 🔴 الصلاحيات — ثلاث قواعد نطاق
```
الطالب  → فرعه ∩ اشتراك ساري
المعلم  → المواد المسندة له فقط (لا يرى مواد زملائه ولا طلابهم)
الأدمن  → غير مقيّد، لكنه لا يُنشئ محتوى تعليمياً
```
أي استعلام لا يطبّق نطاقه = **ثغرة صلاحيات**. الفلترة في الـ Query لا في الـ View.

### 🔴 التصميم
- كل لون من `DESIGN.md` — **لا قيم hex في الكود**
- `{colors.primary}` نادر · `{colors.accent}` الكهرماني للتقييم حصراً
- ارتفاع سطر ≥1.7 للنص العربي · لا `letter-spacing` على العربية أبداً
- زر أساسي واحد لكل شاشة · `EmptyState` لكل قائمة · `ConfirmDialog` لكل حذف

### 🔴 الأمان
- كل حذف = **soft delete** (سلة المحذوفات ركن أساسي في المشروع)
- ملفات المكتبة والفيديوهات **لا تُخدَّم من `public/`** — تمر عبر controller يفحص الصلاحية والاشتراك
- تحقّق من الصلاحية في الـ **Policy**، لا في الواجهة فقط

---

## 4. أسلوب العمل

### قبل أي ميزة
استخدم سكيل `brainstorming` — لا تقفز للكود. المشروع فيه **قرارات معلّقة حرجة** (القسم 7 من وثيقة الفهم)؛ لو لمست ميزة تعتمد على قرار غير محسوم، **اسأل أولاً**.

### أثناء البناء
- ابدأ من المكوّن لا من الشاشة. 48 شاشة = 38 مكوّناً؛ ابنِ المكوّن مرة واستخدمه ٢٠ مرة
- Blade Components في `resources/views/components/`
- Livewire فقط عند الحاجة لحالة تفاعلية حقيقية — لا تجعل كل شيء Livewire
- التزم بـ `php artisan pint` قبل اعتبار أي شغل منتهياً

### قبل ادعاء الإنجاز
استخدم سكيل `verification-before-completion`. **دليل قبل الادعاء دائماً** — شغّل الأمر وأرِ المخرجات، لا تقل "تم" بلا إثبات.

### التواصل
- بالعربية، مباشرة، بلا إطراء
- لو وجدت خطأً في طلب المستخدم: قل الملاحظة بسطرين ثم أكمل الشغل
- لا تخترع قرارات نيابة عن المستخدم في الأمور المعلّقة — اسأل

---

## 5. السكيلز المثبّتة

في `.claude/skills/` — **27 سكيل** (كامل `superpowers` + كامل `pure-skill-suite`)، مُعاد توجيه مساراتها لتعيش داخل المشروع.

**الأكثر صلة يومياً:**
| السكيل | متى |
|---|---|
| `brainstorming` | قبل أي ميزة جديدة |
| `understand` | عند غموض المتطلبات |
| `writing-plans` → `executing-plans` | مهام متعددة الخطوات |
| `systematic-debugging` | أي bug — الجذر قبل العَرَض |
| `test-driven-development` | كتابة ميزة أو إصلاح |
| `verification-before-completion` | قبل قول "تم" |
| `refine` | مراجعة ذاتية للمخرجات |
| `doctrine-keeper` | التقاط قاعدة مكتسبة |
| `handoff` | عند امتلاء سياق الجلسة |

**سكيلز مثبّتة لكنها لا تعمل حالياً:**
`using-git-worktrees` · `finishing-a-development-branch` → تحتاج `git init`
`3d-modeler` → يحتاج `three` + `threebox-plugin` · `montage-creator` → يحتاج `ffmpeg`

التفاصيل والتعديلات المطبّقة: `.claude/skills/README.md`

---

## 6. أوامر متكررة

```bash
php artisan serve
```
```bash
npm run dev
```
```bash
php artisan pint
```
```bash
php artisan test
```

---

## 7. تنبيهات قائمة

- ⚠️ **المشروع ليس مستودع git.** 48 شاشة بلا نسخ احتياطي = مخاطرة عالية جداً. `git init` أولوية.
- ⚠️ **Livewire 3 غير مثبّت بعد** — `composer require livewire/livewire`
- ⏳ **الهوية البصرية لم تصل** — ألوان `DESIGN.md` مبدئية، تُستبدل من الـ frontmatter فقط
- ⏳ **قرارات حرجة معلّقة:** نطاق الاشتراك (م-1) · سلوك الكويز (م-2) · شاشات الدفع (م-4) · استضافة الفيديو (م-5)
