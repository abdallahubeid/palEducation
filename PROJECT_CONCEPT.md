# PROJECT_CONCEPT.md — palEducation

> **الحالة:** وثيقة حيّة — تُحدَّث كلما تغيّر فهمنا للمنتج أو حُسم قرار معلّق.
> **آخر تحديث:** 2026-08-19
> **الغرض:** مصدر الحقيقة الواحد لفكرة المنصة، أهدافها، جمهورها، وميزاتها الأساسية — قبل الغوص في تفاصيل أي شاشة.
> **لا يكرّر:** التفاصيل المعمارية والشاشات موجودة في `docs/01-03`. هذا الملف يجيب "لماذا ولمن"، تلك الملفات تجيب "كيف بالتفصيل".

---

## 1 · الفكرة في جملة واحدة

> منصة تُقصي عن الطالب كل ما لا يخص فرعه، وتغلق حلقة التعلّم (شرح ← تقييم) داخل الشاشة نفسها.

palEducation منصة تعليمية إلكترونية لطلبة **الثانوية العامة (التوجيهي) في فلسطين** — جمهور تحت ضغط امتحان مصيري واحد، يفتح المنصة مساءً بعد يوم مدرسة طويل ويحتاج أن يصل لما يخصه **بلا احتكاك وبلا تشتّت**.

---

## 2 · المبدآن الحاكمان

كل قرار منتج أو تصميم لاحق يُقاس على هذين المبدأين. أي ميزة تخالفهما تُعاد للنقاش قبل البناء.

| المبدأ | ماذا يعني عملياً |
|---|---|
| **الإقصاء المخصص** | الطالب لا يرى محتوى خارج فرعه إطلاقاً — لا في القوائم، لا في البحث، لا في المكتبة. الفرع فلتر جذري على **كل** استعلام وكل واجهة، لا خيار عرض. |
| **الحلقة المغلقة** | لا انتقال يدوي من الفيديو للكويز. انتهاء المحاضرة **حدث** يفتح الكويز تلقائياً — الطالب لا يضغط "ابدأ الاختبار". |

---

## 3 · لمن نبني — الأدوار الأربعة

| الدور | نطاقه | ما يميّزه |
|---|---|---|
| **زائر** | المحتوى العام فقط | يُقنَع بالمنصة قبل إنشاء حساب — الصفحة الرئيسية وظيفتها هذه بالكامل |
| **طالب** | `فرعه` ∩ `اشتراك ساري` | الجمهور الأساسي؛ يقضي أطول وقت على المنصة |
| **معلم** | `المواد المسندة له` فقط — لا يرى مواد زملائه ولا طلابهم | ينشئ المحتوى (محاضرات + كويزات)، لا يديره |
| **Super Admin** | غير مقيّد | يراجع ويحذف ويسند — **لا يُنشئ محتوى تعليمياً** |

القاعدة الأمنية الحاكمة: أي استعلام يعرض محتوى دون تطبيق فلتر الفرع + الاشتراك = ثغرة. التفاصيل الكاملة: [docs/01-project-understanding.md](docs/01-project-understanding.md) §3.

---

## 4 · القيمة المقترحة — لماذا يبقى الطالب

1. **لا ضوضاء.** فرعه فقط، مواده فقط. لا يتصفّح محتوى لن يمتحن فيه أبداً.
2. **حلقة مغلقة فوراً.** يشاهد المحاضرة → الكويز يفتح تلقائياً → يعرف مستواه لحظياً، بلا خطوة يدوية تُنسى أو تُؤجَّل.
3. **المكتبة المنظّمة** ⭐ — نقطة التمايز التنافسي. أوراق عمل وملخّصات وامتحانات سابقة مجمّعة بفرعه، لا مبعثرة عبر مجموعات فيسبوك وتيليجرام كما هو الوضع الحالي لمعظم طلبة التوجيهي.
4. **الأحمر يوضّح ولا يوبّخ.** نتيجة كويز خاطئة تُعرض بجانب الإجابة الصحيحة فوراً — بلا أيقونات فشل صارخة، بلا اهتزاز، بلا صوت. جمهور تحت ضغط نفسي عالٍ لا يحتاج مزيداً من القلق.

---

## 5 · شجرة المحتوى — العمود الفقري

```
الفرع (Branch)              ← علمي · أدبي · تجاري · صناعي
 └── المادة (Subject)              ← رياضيات · فيزياء · لغة عربية...
      │   └── مُسندة إلى معلم
      ├── المحاضرة (Lecture)  [فيديو + عنوان + وصف]
      │    └── الكويز (Quiz)   ← علاقة 1:1 إجبارية عند النشر
      │         └── السؤال → الخيار + الإجابة الصحيحة
      └── ملف المكتبة (Library File)  ← أوراق عمل · ملخصات · امتحانات سابقة
```

ملفات المكتبة تُعرض في مكانين بمنظورين مختلفين (صفحة المادة: "أدرس هذه المادة الآن" · المكتبة: "أريد كل ملفات فرعي مجمّعة") — هذا تصميم مقصود لنقطة التمايز، وليس تكراراً.

---

## 6 · الستاك التقني

```
Laravel 13.17 · PHP 8.3 · Livewire 3 · Tailwind CSS 4 · Vite 8 · Pest 5 · MySQL (XAMPP)
```

- Tailwind 4 عبر `@theme` في `resources/css/app.css` — لا `tailwind.config.js`
- Blade + Livewire فقط عند الحاجة لحالة تفاعلية حقيقية — Blade الساكن هو الافتراضي
- ثنائي اللغة: عربي RTL أساسي / إنجليزي LTR ثانوي — logical properties حصراً، لا `left`/`right`

التفاصيل: [CLAUDE.md](CLAUDE.md) و[.claude/rules/](/.claude/rules/).

---

## 7 · أين نقف الآن

| البند | الحالة |
|---|---|
| نظام التصميم | ✅ معتمد v2.2 — لوحة edunova (بنفسجي `#525fe1` + كهرماني `#f0a500`)، موثّق في [DESIGN.md](DESIGN.md) |
| الشاشات المبنية | ✅ **12 شاشة** واجهة أمامية (بيانات وهمية، بلا خادم): الرئيسية · لوحة الطالب · موادي · صفحة المادة · مشاهدة المحاضرة (فيديو+كويز+نتيجة مُضمَّنة) · كويز/نتيجة مستقلان (احتياطي) · المكتبة · تقدّمي · اشتراكي · ملفي · الإشعارات. التفصيل: [docs/03-screens-detail.md](docs/03-screens-detail.md) |
| المكوّنات | ✅ **~45 مكوّن Blade** — الجرد الكامل في [DESIGN.md](DESIGN.md) |
| Livewire 3 | ❌ غير مثبّت بعد — كل التفاعل الحالي Blade + vanilla JS (`resources/js/app.js`)، لا Alpine.js |
| قاعدة البيانات | جداول Laravel الافتراضية فقط — لا Models ولا migrations ولا seeders خاصة بالمشروع |
| المصادقة والصلاحيات | غير موجودة بعد |
| الاختبارات | لا اختبار خاص بالمشروع — فقط سكرافولد Pest الافتراضي |
| المرحلة الحالية | **الموجات 1 و2 مكتملتان، والموجة 5 مكتملة بالكامل** (+ شاشتا اشتراكي وملفي من الموجتين 3/4 خارج الترتيب). القرار التالي: الاستمرار أفقياً بشاشات جديدة، أم التحوّل عمودياً لربط المبني فعلياً بخادم حقيقي (Livewire + قاعدة بيانات + مصادقة) — لم يُحسم بعد. التفصيل: `docs/01-project-understanding.md` §10 |

---

## 8 · خارطة الطريق رفيعة المستوى — 9 موجات، 48 شاشة

```
الموجة 0 → الأساس            (0 شاشة)  ✅ Design Tokens معتمدة — 4 قشرات مبنية جزئياً، /styleguide لم يُبنَ
الموجة 1 → المرجعية          (3)       ✅ مبنية كاملة
الموجة 2 → الحلقة المغلقة    (3)       ✅ مبنية (مُضمَّنة بشاشة واحدة فعلياً، لا 3 صفحات — انظر docs/02 §الموجة 2)
الموجة 3 → مسار التحويل      (6)       ⏳ شاشة واحدة فقط (اشتراكي) · الباقي معلّق على م-1/م-4
الموجة 4 → الدخول والهوية    (5)       ⏳ شاشة واحدة فقط (ملفي) · لا مصادقة بعد
الموجة 5 → بقية منطقة الطالب (4)       ✅ مبنية كاملة — المكتبة ⭐ ضمنها
الموجة 6 → بقية المنطقة العامة(4)      ⏳ لم تُبنَ
الموجة 7 → منطقة المعلم      (8)       ⏳ لم تُبنَ
الموجة 8 → لوحة الأدمن       (11)      ⏳ لم تُبنَ — الأسرع تنفيذاً لاحقاً (نمط جدول متكرر)
الموجة 9 → النظام والسلة     (4)       ⏳ لم تُبنَ
                            ─────
                              48        — 12 مبنية فعلياً
```

أول 12 شاشة (الموجات 1–3) تغطي المنتج القابل للإطلاق: طالب يتصفّح، يشترك، يشاهد، يُختبر. الباقي توسعة. التفصيل الكامل والمُحدَّث: [docs/02-ui-design-flow.md](docs/02-ui-design-flow.md).

---

## 9 · القرارات المعلّقة الحرجة

هذه تحجب موجات كاملة ولا يجوز البناء فوقها بافتراض — **تُسأل لا تُخترع** ([.claude/rules/doctrine.md](.claude/rules/doctrine.md)).

| # | القرار | يحجب | التوصية المسجّلة |
|---|---|---|---|
| م-1 | نطاق الاشتراك: بالمادة أم بالفرع؟ | الموجة 3 كاملة | اشتراك بمستوى الفرع |
| م-2 | سلوك الكويز (محاولات، هل يقفل المحاضرة التالية) | الموجة 2 (شاشتا الكويز والنتيجة) | مشاهدة ≥90٪ لفتح الكويز · محاولتان · لا قفل |
| م-3 | هل الكويز إجباري لنشر المحاضرة؟ | بناء الكويز (شاشة المعلم) | إجباري — هو الوعد الأساسي للمنصة |
| م-4 | آلية الدفع (المحتوى الفلسطيني غالباً دفع يدوي) | 3 شاشات دفع + مراجعة المدفوعات | — بانتظار قرار العمل |
| م-5 | مزوّد استضافة الفيديو | شاشة مشاهدة المحاضرة | Bunny Stream أو Vimeo، لا يوتيوب |
| م-6 | تغيير الفرع بعد الاختيار الأول | ملفي الشخصي (طالب) | تغيير عبر طلب للأدمن فقط |

التفصيل الكامل لكل قرار (بما فيها م-7 حتى م-12 الثانوية): [docs/01-project-understanding.md](docs/01-project-understanding.md) §7.

> ⚠️ **لا قرار من هذا الجدول محسوم فعلياً حتى الآن — رغم بناء الموجة 2 وأجزاء من 3/4.** الشاشات المبنية التي تلمس م-2 (الكويز) وم-5 (الفيديو) استخدمت "التوصية المسجّلة" أعلاه لبناء **محاكاة عرض** (localStorage، زر محاكاة انتهاء فيديو) لا حسماً فعلياً. أي منطق خادم أو قاعدة بيانات حقيقية على هذين القرارين يحتاج سؤال المستخدم وحسماً صريحاً أولاً — لا البناء على الافتراض. راجع `.claude/rules/doctrine.md` § القرارات المعلّقة تُسأل لا تُخترع.

---

## 10 · كيف تُحدَّث هذه الوثيقة

- عند حسم أي قرار معلّق (القسم 9): انقل السطر من "معلّق" إلى "محسوم" هنا، وحدّث `docs/01` بنفس الوقت.
- عند اكتمال موجة: حدّث القسم 7 (أين نقف الآن) والقسم 8 (الخارطة).
- لا تُكرَّر هنا تفاصيل شاشة واحدة — تلك مكانها `docs/03-screens-detail.md`. هذا الملف يبقى على مستوى "المنتج والاتجاه"، لا "البكسل والمكوّن".
